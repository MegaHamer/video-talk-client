import { Participant } from "@/shared/hooks/mediasoup/paricipants/Participant";
import { memo } from "react";

const ParticipantVideo = memo(({ participant }: { participant:Participant }) => {
    
  return <div></div>;
});
